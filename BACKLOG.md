# Prism — роадмап к публичному web-релизу

> **Как вести этот файл (читать первым после компакта; карта репо — `ARCHITECTURE.md`).**
> Единственный источник правды по «куда идём, что делаем и что решили». Правила:
> (1) **самодостаточность** — никаких ссылок на чат/память; (2) активная работа — в
> майлстоунах явными чекбокс-задачами с критерием готовности; (3) каждое принятое
> дизайн-решение — в «Журнал решений» одной строкой; (4) **выполненное и устаревшее
> УДАЛЯЕМ**, а не помечаем «сделано» — история живёт в git; (5) **роадмап — карта,
> не клетка**: спонтанная хотелка пользователя всегда приоритетнее очередного пункта
> майлстоуна — вклиниваем сразу, майлстоуны сдвигаются, отклонение фиксируем строкой
> в журнале. Роадмап отвечает на «что дальше, когда нет явной хотелки» и охраняет
> одно: фундамент M1 не размывать — он окупает всё остальное.

## Цель

**Публичная web-версия Prism**: браузерный клиент (Godot HTML5), VPS, аккаунты,
матчмейкинг, полные сеты 5 цветов, колодостроитель, AI-противник, режимы
(драфт/арена), прогрессия (ранги/титулы). **Все карты доступны всем по умолчанию**
(нет коллекции/анлока карт → ноль pay-to-win); экономика — **только косметика**
(рамки/борды/скины героев) за мягкую валюту, с тирами редкости и колесом/лутбоксами.

**Состояние на старте роадмапа (2026-06):** играбельный e2e-стек — C++-движок
(82 теста, грамматика эффектов, 22 ключевика, 4 пассивки героев), WebSocket-сервер
приватных комнат (код+пароль), Godot-клиент (координатор + модули, 40 headless-тестов,
CI). 69 карт, 4 героя. Рефакторинг клиента завершён (Main 1661→836, слой `Rules`,
сигналы-намерения). Чего НЕТ: сериализации состояния, перечисления легальных ходов,
реконнекта, бота, настоящих колод (играет `demoDeck` — по 1 копии всего сета),
колодостроителя, аккаунтов/персистентности, web-экспорта, TLS, адаптивного UI,
звука (вообще), EN-имён, косметики/экономики.

## Целевая архитектура

```
[Браузер: Godot HTML5]──┐
[Десктоп: Godot]────────┤ wss (JSON, versioned)
                        ▼
        [reverse-proxy: caddy — TLS, COOP/COEP, статика клиента]
                        ▼
        [prism_server (C++, один процесс)]
          ├─ лобби: приватные комнаты + открытая очередь матчмейкинга
          ├─ Game-движок (авторитарный; редактированные view)
          ├─ Bot (в процессе, занимает второе кресло)
          ├─ сериализация партий → реконнект, реплеи (seed+лог действий)
          └─ мета-слой → [SQLite]: аккаунты(токены), колоды, прогрессия,
                          инвентарь КОСМЕТИКИ (карты не коллекционируются), история
```

Принципы: **один сервер-процесс** (не плодим сервисы — SQLite, бот и мета живут
внутри `prism_server`); **сервер никому не верит** (валидация колод и действий);
**движок детерминирован** (seed + лог действий = реплей); клиент — тонкий рендер
view (это уже так — сохранять).

## Архитектурные решения на будущее (зафиксировано, чтобы не переделывать)

1. **Сначала фундамент движка, потом контент.** Перечисление легальных действий и
   сериализация состояния разблокируют сразу: бота, реконнект, фаззинг, реплеи,
   админ-панель. Делать ДО контент-рывка.
2. **Реплей = seed + лог действий.** Движок детерминирован (`Game(lib, deck0, deck1,
   seed)`); полная сериализация нужна только для реконнекта/админки, реплеи дешевле.
3. **Карты НЕ имеют редкости и НЕ коллекционируются** — все доступны всем по
   умолчанию (ноль pay-to-win; проще бэкенд — нет коллекции на аккаунт). Редкость
   живёт на КОСМЕТИКЕ (рамки/борды/скины), это отдельный тип данных, `CardDef` не
   трогает. `name.en` — конвенция контент-трека (добавляем к каждой карте по мере
   написания, чтобы не бэкфиллить; локализация — пост-релиз).
4. **Версия протокола.** Поле `v` в первом сообщении клиента — ДО появления
   публичных клиентов (старые браузерные клиенты живут в кэшах).
5. **Колоды валидирует сервер** (40 карт, ≤2 копии, существующие id) — `readLoadout`
   сейчас принимает что угодно; это чит-вектор. Правило одно, живёт в движке/сервере.
6. **Бот — в процессе сервера**, не отдельный клиент: садится на seat 1 комнаты
   «vs AI». Интерфейс бота = перечисление легальных действий + оценка.
7. **Web-экспорт**: wss обязателен (https-страница не может ws://), за caddy;
   threads-экспорт Godot требует заголовков COOP/COEP — caddy их отдаёт.
8. **Экономика — только мягкая валюта** (никаких реальных денег в лутбоксах без
   отдельного юр-анализа); честность дропа (pity) фиксируется в дизайне сразу.
9. **Звук обязателен до публики** (сейчас 0 аудио): SFX на события + эмбиент.
10. Godot 4 остаётся; позиционной боёвки нет (решено ранее).

## Майлстоуны (последовательные)

Размеры: S — день-два сессий, M — несколько, L — много, XL — фоновый поток.

### M1 — Фундамент движка (L) ✓ закрыт
legalActions / сериализация / реплеи / фазз / лог действий в комнате (`Room` пишет
принятые действия → `makeReplay`, дамп в `replays/` на game-over; тот же лог питает
реконнект M5). ctest зелёный с фаззом, реплей бит-в-бит.

### M2 — Дев-ускорители (M)
Зачем: резко удешевляет проверку контента и багов (сейчас сценарий = сыграть партию).
- [ ] Админ-панель (dev-сборка): `debug/setState` на сервере (принимает
      `Game::fromJson`) + клиентский редактор сценария (борд/рука/мана/hp/ход).
- [ ] Лог действий в UI (лента «что произошло»): сервер шлёт событие-сообщения
      рядом с view (или клиентский дифф) — видно эффекты соперника и цепочки.
- [ ] Реплей-просмотр в клиенте (минимально: прокрутка действий по логу из M1).
Критерий: любой игровой сценарий воспроизводится за <1 минуты без живой партии.

### M3 — Бот и баланс-пайплайн (L)
Зачем: одиночный режим + автоматическая проверка баланса для контент-рывка.
Бот v1.5 (жадный рефлекс + 1-ply поиск), режим «Тренировка» и self-play CLI
(`prism_selfplay`: параллельный по ядрам, 1000 партий ≈ 4 мин в Release; отчёт —
перекос порядка хода, winrate героев, GIH-винрейт карт; + json) — СДЕЛАНО.
ВАЖНО: собирать в **Release** (`-DCMAKE_BUILD_TYPE=Release`), иначе поиск в 8× медленнее.

**Известное ограничение: бот full-information («подглядывает»).** Бот читает авторитарный
`Game` напрямую (не редактированный view), поэтому при 1-ply симуляции ответа соперника
видит его НАСТОЯЩУЮ руку и порядок колоды. Для self-play (оба читерят симметрично) и
тренировочной дамми — приемлемо. Для ЧЕСТНОЙ игры против человека нужна **детерминизация**:
скрыть чужую скрытую инфу и сэмплировать правдоподобную руку из оставшегося пула
(MCTS-style по нескольким гипотезам), либо убрать rollout соперника (оценивать только
конец своего хода). Будущая работа, не блокер.

### M4 — Колодостроитель + валидация колод (M)
- [ ] Экран конструктора: сетка ВСЕХ карт (все доступны — нет коллекции; фильтры
      цвет/тип/стоимость/ключевик), сборка 40/≤2 с живой валидацией и кривой маны.
- [ ] Серверная валидация колод: 40 карт, ≤2 копии, валидные id, не-hero; отказ →
      `joinError`. Единое правило в движке/сервере. (Перенесено из M1 — валидацию
      делаем вместе с билдером, иначе негде взять легальную колоду.)
- [ ] Decks: пресеты-архетипы (моно-цвета + 2-3 сплава) — играбельны без билдера.
- [ ] Дев-песочница: текущая «1 копия каждой карты» остаётся как dev-only
      (валидация байпасится) для теста всего сета; `demoDeck` живёт до этого этапа.
- [ ] Локальное хранение колод (`user://decks.cfg`, формат как `Decks`), выбор в лобби.
Критерий: собрал колоду → сохранил → сыграл ей с другом и с ботом; кривая/мусор
от чужого клиента отбиваются.

Реализация фазами:
1. ✓ **Единое правило** `validateDeck` в движке (`engine/include/prism/deck.hpp`:
   `kDeckSize=40`, `kMaxCopies=2`, не-hero, валидные id) + **серверный энфорс**: непустая
   колода в create/join валидируется, невалидная → `joinError "bad_deck"` (детали
   `reason:detail`, reason ∈ size|copies|unknown|hero); пустая → dev-фолбэк `demoDeck`.
   doctest `deck validation rule` зелёный.
2. Клиент: `deck_rules.gd` (зеркало правила) + экран `deck_builder.gd` (сетка карт,
   фильтры цвет/тип/стоимость/ключевик, +/−, счётчик 40/≤2, кривая маны, **валидация в
   момент сохранения**) + хранение `user://decks.cfg` + выбор в лобби.
3. Пресеты-архетипы (5 цветовых легальных колод 40/≤2 + сплавы).

### M5 — Web-готовность и деплой (L)
Цель этапа — **десктоп-браузеры** (мышь); мобилки/тач — отдельный пост-релизный этап.
- [x] Godot web-экспорт (HTML5): собран, ~180МБ, однопоточный → COOP/COEP не нужны;
      пресет `Web` закоммичен. Клиент сам коннектится к same-origin `wss://host/ws`.
- [x] **Turnkey-деплой готов и проверен локально** (`deploy/`): один параметризованный
      `Caddyfile` (TLS+статика+wss-прокси) обслуживает оба пути —
      **Docker Compose** (`Dockerfile` собирает сервер, `docker-compose.yml`:
      prism+caddy, клиент монтируется томом, образ сервера 78МБ) и **bare systemd**
      (`prism-server.service`). Полный стек прогнан в контейнерах: caddy отдаёт
      клиент + `/ws`→сервер, матч идёт. `deploy/local_serve.py` — прод-зеркало без VM.
- [ ] **Завести VM + хостнейм (действие пользователя):** Oracle Always Free
      (10ТБ egress) или дешёвая VPS (~$4–6/мес) + DuckDNS/домен (A-запись на IP).
      Затем по `deploy/README.md` §2A: `docker compose up -d --build`. Заодно
      проверить коллизии имени «Prism» (игры/трейдмарки) — возможен ребренд витрины.
- [ ] Харднинг сервера: idle-таймауты сокетов, GC пустых комнат, rate-limit
      сообщений, лимит комнат на IP. (Лимит кадра 1 MiB уже есть в `ws.hpp`.)
      Текущее состояние ок для приватных комнат: брошенная комната чистится при
      обрыве хоста (POLLHUP→dropClient), одна комната на сокет.
- [ ] Реконнект: токен места (room+seat+secret), при обрыве клиент переподключается
      и получает текущий view (сериализация из M1); таймер дисконнекта → техлуз.
- [ ] Поле `v` протокола + отказ несовместимым клиентам.
Критерий: друг открывает URL в браузере и играет; обрыв сети не убивает матч.

### M6 — Мета-сервер: аккаунты и матчмейкинг (L)
- [ ] SQLite в `prism_server`: players (токен-аккаунты, ник), decks, cosmetics
      (инвентарь), match_history. **Карты не коллекционируются** — таблицы коллекции
      нет, все карты у всех. Миграции — простые versioned-скрипты.
- [ ] Лёгкие аккаунты: анонимный токен при первом входе (хранится клиентом),
      ник; почта/пароль — позже, при необходимости.
- [ ] Открытая очередь «Быстрая игра» рядом с приватными комнатами.
- [ ] Колоды переезжают с клиента на сервер (клиентский кэш остаётся).
Критерий: два незнакомых игрока находят друг друга кнопкой; колоды живут в аккаунте.

### M7 — Режимы и прогрессия (L)
- [ ] Драфт/арена: серверная раздача пиков (пулы — по цвету/стоимости, редкости у
      карт нет), драфт-колода живёт в аккаунте до конца забега; счёт побед.
- [ ] Ранги: ладдер по сезонам, высший ранг = титул «Испытание Мастерства».
- [ ] Базовые квесты/дейлики (опционально, питает экономику M8).
Критерий: полный забег арены и видимый ранг в профиле.

### M8 — Экономика и кастомизация (XL, дизайн-тяжёлый)
**Только косметика — геймплей (карты) весь бесплатен → ноль pay-to-win.**
- [ ] Мягкая валюта из побед/квестов; косметика покупается/открывается за неё.
- [ ] «Лудоманские» ивенты: колесо фортуны, лутбоксы — с честным pity, дроп —
      **только косметика** (рамки/борды/скины/эффекты), с тирами редкости.
- [ ] Скины: рамки карт, борды, альтернативные арты героев (пайплайн MJ есть).
Критерий: новый аккаунт проходит путь «играю → зарабатываю → открываю косметику»;
никакой контент-стены, все карты доступны с первого матча.

## Параллельный трек К — контент (XL, фоновый)

Полные сеты: цель **~125–150 карт** (25–30 на цвет с двуцветками), **герой(и) на
каждый цветовой архетип** (сейчас 4; цель 6+). Правила трека:
- Батчи по 10–15 карт; каждый батч: дизайн → `name.en`/арт-промпты
  (`tools/build_prompts.py`) → `balance.py` → **после M3** self-play отчёт → правки.
- Равенство типов сета (см. Журнал решений) держим в каждом батче.
- Новые ключевики — по кукбуку `ARCHITECTURE.md A7`; кандидаты на возврат — см.
  «Вырезанные ключевики»; `ambush`/`refract` требуют триггеров вне своего хода
  (механика для отдельного захода).
- До M3 батчи маленькие и осторожные (баланс вручную), после M3 — конвейер.

### Реворк сета — ГОТОВО (2026-06, пересобран набело)
Сет: **85 карт** = **12 моно на цвет** (60) + **2 на каждую из 10 пар** (20) +
**5 призматик**. Нейтральных карт НЕТ. Героев 4 (отдельная ось, exempt от правил ниже).

**Распределение кейвордов — ровное.** Каждый из 29 цветовых кейвордов встречается
**3-5 раз** (цель 4). Держать в банде при любых правках. ambush/refract/umbra — нет в
движке, в сете не используются.

**Без клонов.** Никакие две карты не делят полный пакет (статы+цена+ключевики+эффекты);
повтор кейворда — только в РАЗНОЙ упаковке. (Был набор из ~20 карт-клонов под разными
именами — пересобраны в уникальные.)

**Параметр N — разводить, не лепить везде 1.** У параметрических кейвордов N должен иметь
диапазон, привязанный к стоимости (дороже → выше N): growth/compost/delay/regen/undergrowth/
chill/decoy/scry/split/spores/photosynthesis/freeze/blind несут N=1..3. Исключения, осознанно
держим N=1 (N>1 ломает баланс/снежный ком): **resonance** (множитель на число кристаллов →
карта обязана быть 1/1, иначе двойной счёт), **flash** (масс-ослепление на 2 хода — давящее),
**germinate** (дешёвые токены). growth:2 — только на самой дорогой карте (призматик-флагман).
**decoy/awaken — только на дешёвых картах** (на 7-10-мане decoy = бесплатный розыгрыш в обход
цены; снят с обеих призматик).

**Нейтральные эффекты — не пул карт, а райдеры по цветам** (вне правила ровности):
- `draw` и `dispel` — в КАЖДОМ цвете. Рука горит (мана-через-жертву карт → −1 карта/ход),
  а ауры без сайдборда надо отвечать всем → как бесцветный сплэш-ответ в MTG, но размазано.
- `damage` / `damage_all` → 🔴 (огонь: фокус-луч и взрыв). `destroy` → 🟣 (УФ-развоплощение).
  Урон и хард-килл — цветные специальности, не у всех. `add_crystal` НЕ используем.

**Цвето-идентичность:** каждая цветная карта несёт свойство своего цвета; utility-карты
(только draw/dispel) — exempt (цвет = доступ к мане, не идентичность). Чисто.

**Имена:** `id == <цвета>_<snake(en)>` — жёстко, страж `tools/check_ids.py` (герои exempt).
Имена 2 слова RU+EN. Существо = КОНКРЕТНАЯ ТВАРЬ (не феномен — иначе MJ выдумывает медузу),
спелл/аура = явление. Имя↔эффект↔арт = один субъект.

**Генератор артов (`tools/build_prompts.py`), правила зашиты комментарием:**
- стиль жёстко плоский-2D (cel-shaded, `--no 3D/CGI/photo/...`); поле `STYLE_REF` под
  `--sref` — единственный надёжный лок стиля между картами (текстом v7 гуляет);
- всё свечение привязано к цвету карты (`green light/energy/...`) → нейтральный свет НЕ
  течёт в синь (была главная утечка цвета);
- моно-карта = любой оттенок своего цвета (свободно), `NEG` гасит оттенки 4 чужих цветов;
  RANGE и NEG из одного `SHADES`.

**Баланс — Tier-1 модель** (`balance.py`): статы `0.6·atk + 0.467·hp − 0.734` (atk≈1.3×hp,
заякорено на ушедшей ваниль-кривой 3/2@2, 4/5@4); `shield` масштабируется телом.
Набор: **0 флагов**, sd 0.30. План улучшения (Тир 2 self-play и т.д.) — ниже отдельно.

Фикстуры обновлены (тест `sample.json`-schema → `dim_gleam`; devkit → `green_ancient_lightwood`,
`blue_deep_study`). Все 85 артов сгенерены и импортированы.

Мелочь: генератор текста показывает delay чипом «Отсрочка N» + отдельной строкой эффект,
не склеивает в «через N ходов: …» (как flash у sunstroke) — косметика, тултип поясняет.

### Система баланса — план улучшения (решено 2026-06)
Текущий `balance.py`: `C = мана + наценка_мультиколор`; `P = статы + Σкоэфф.ключевиков
и эффектов`; `R = P − C`, флаг при |R|>0.75. Коэффициенты руками «на глаз». Слабости:
линейна и без контекста (не видит синергий/убывающей отдачи/антисинергий), статы были
примитивны (atk=hp), нет темпа/кривой маны, и главное — **самоподтверждающаяся**:
«0 флагов» = «согласуется с нашими интуициями», а не «сбалансировано». Не находит
дисбаланс, который мы сами не заложили (светодрев-бомба, недооценка growth/self_lifesteal
ловились пост-фактум руками).

План по тирам:
- **Тир 0:** `balance.py` — это линтер-априор / смоук-тест, НЕ истина. Истина — исходы партий.
- **Тир 1 (ГОТОВ 2026-06):** atk/hp-split (atk≈1.3× hp; заякорено на ванильных
  `faceless_golem` 3/2@2 + `radiant_monolith` 4/5@4 → оба R≈0); защитные ключевики
  (`shield`) масштабируются телом (как уже делают split/haunt/bypass). Выпуклость по
  мане — уже НЕЯВНО в offset статов (большие ванили слегка слабы, дешёвые честны);
  явный knob отложен до Тир-2-фита (2 ванильных якоря линейны, не дают заякорить).
  Ручные синергии НЕ вводим — фейк; настоящие придут из регрессии Тир-2.
- **Тир 2 (главный рычаг):** в движке уже есть `legalActions`+`applyAction`+фазз+`replay`
  → собрать self-play (random/greedy → лёгкий MCTS), гонять тысячи партий, мерить вклад
  карты (Δ-винрейт с/без, play-rate, tempo-swing, частота в выигрышных бордах).
  Коэффициенты ПОДГОНЯТЬ регрессией (присутствие карт → маржа победы), не руками.
- **Тир 3:** телеметрия реальных плейтестов в ту же регрессию; ночная переподгонка +
  флаг «дрейфующих» карт; срезы по архетипам (карта ок в мидрейндже, сломана в агро).

**Первый Тир-2-проход (2026-06, журнал):** прогнали `prism_selfplay` 1000 партий, фит
коэффициентов под исходы. Что выяснили — и почему остановились на «достаточно»:
- Метрика «карту РАЗЫГРАЛИ» даёт ложную драму (винрейты 33–87%): это selection-bias
  (дорогую карту считаем только в партиях, где дожили до её маны) + agency-bias (бот
  редко играет removal). Переключили на **GIH — «карту ВЫТЯНУЛИ в руку»** (обуславливаемся
  на доборе, не на розыгрыше). Это правильная мера, оставлена в CLI.
- По чистому GIH **пул в целом сбалансирован**: вне шума (|z|>2.5, SE≈2.4pp при n≈440)
  всего 7 карт из 85. corr(R, GIH)≈0.2 — функция и исходы согласованы слабо, но и
  выбросов почти нет: тюнить по такому сигналу = ловить шум.
- Единственный структурный сигнал — **все три 1-мановых существа = топ-3** (z=+4.4..+5.8),
  независимо от ключевика. Но это **артефакт бота, не сила карт**: мулиган бота держит
  дешёвые (выбрасывает manaValue≥4), а в ману он жертвует самое дешёвое существо —
  поэтому дешёвое тело раздувает винрейт как «глаже рука + идеальный корм для рампы»,
  а не как боец. Карты НЕ трогали.
- Вывод первого прохода: GIH над ПОЛНЫМ пулом нечувствителен — каждая партия содержит
  каждую карту → нулевая вариация присутствия. A/B-тест добил: крупный нерф 6 карт сдвинул
  GIH случайно в шуме (sd≈2.45pp, |z|<2). Метрика не годна для карт. Калибровка `balance.py`
  (бамп regen/shield/germinate/growth/compost/undergrowth/split + пологий резонанс
  `n·(0.7·мана+1.8)`) оставлена — согласуется с интуициями сета.

**Второй Тир-2-проход — НАСТОЯЩАЯ метрика (2026-06, СДЕЛАНО Фазы 0–2):**
Диагноз починен — мерить надо над СЛУЧАЙНЫМИ колодами, не над полным пулом (тогда карта
то есть, то нет → появляется контраст), а перекос сидений вычитается дифференцированием.
- **Харнесс** (`prism_selfplay --draft`): на партию драфтит цвето-когерентную колоду 40
  карт (≤2 копии), случайный герой; пишет JSONL по строке на партию (обе колоды, герои,
  winner). Каждая партия — чистая функция сида (драфт+герой+шафл+RNG бота от одного сида)
  → воспроизводима, и A/B на тех же сидах = CRN. ~6 партий/с в Release (2000 ≈ 5.5 мин).
- **Скрин** (`tools/balance_lab.py screen`): дифф-LPM (одна строка/партия, y=победил seat0,
  x_card = копий seat0−seat1) + бутстрап-SE + FDR. β = вклад карты в винрейт в pp — одно
  число на карту. Контроли: интерсепт (перекос 1-го хода) + дамми героев. Автономно.
- **CRN-валидатор** (`balance_lab.py crn`): парный эффект одной правки на одних сидах —
  колоды идентичны, отличается только определение карты → дисперсия схлопывается.
- **ГЕЙТ ПРОЙДЕН:** подсадили нерф (crystal_sapling → 10м 0/1), CRN поймал на **z=+4.52**
  (null-карты |z|<2) — ровно то, чего GIH не мог. Независимый скрин двух прогонов слабее
  (z≈1.2 на этом мелком эффекте): **CRN — для проверки конкретной правки, скрин — для
  поиска крупных выбросов.** Перекос 1-го хода в драфте всего −2.5pp (в полном пуле был
  −6.4..−7.6: случайные колоды делают исход менее зависимым от сидения).
- **Скрин 8000 партий + cost-контроль (СДЕЛАНО):** residual = β минус тренд `β~mana_value`
  (БЕЗ типа — слабость класса бывает реальной: ауры честно слабы из-за обилия диспела,
  и сами диспелы поэтому посредственны → там ВЕРИМ боту, тип не вычитаем). Выделил
  3 непротиворечивых mid-cost кандидата; каждый прогнали через CRN.
- **CRN-проход кандидатов — из 3 подтверждён 1 (важный урок про строгое подтверждение):**
  - `red_ray_ram` (3/3 bypass, −2.8pp) — **слепое пятно бота, НЕ слабая.** Бафф статов
    упирается в +1pp (4/3≈4/4≈5/3, все ~−1.1pp, прямой CRN 4/3↔5/3 z=−0.44). Причина в
    коде: бот оценивает AttackHero как `(ahead?5:2)+dmg` без бонуса за bypass; bypass влияет
    только на ЛЕГАЛЬНОСТЬ (бить героя сквозь provoke, game.cpp:461) → жадный 1-ply не ценит
    неблокируемость. Карта норм для людей, не трогаем.
  - `red_reckless_sparkshooter` (2/2+урон2) — флаг скрина (+1.84) **НЕ пережил CRN**
    (+0.49pp, z=1.18) → ложное срабатывание FDR. Не трогаем.
  - `blue_violet_false_ripple` (3м, mirage+scatter) — **подтверждён** (CRN +0.99pp, z=2.5).
    Единственная правка прохода: **3→4 маны** (generic 1→2).
- **ГАРД keyword-слепоты:** тюнер НАДЁЖЕН для карт, чью ценность бот умеет ВЫРАЗИТЬ (сырые
  статы, простые тела, урон, темпо/вэлью — пересил бот видит хорошо). НЕНАДЁЖЕН для
  навык/контекст-зависимых ключевиков (**bypass, stealth, floodlight, отчасти provoke/
  reach**) — там β мерит «как бот разыгрывает», не «сколько стоит». Такие карты из
  авто-стат-тюнинга ИСКЛЮЧАТЬ. Настоящий фикс — улучшить бота (детерминизация + глубже
  поиск, чтобы ценность ключевиков возникала из планирования, а не из коэффициентов).
- **Осталось (Фаза 3):** propose/`--apply` тюнер (рулить по β, граница — `balance.py` R,
  правки только мана/статы, скип keyword-слепых карт), затем рефит коэффициентов
  `balance.py` под β. Слепое пятно: синергийные карты в случайных колодах недооценены
  (партнёр обычно отсутствует) — взаимодействия/архетипы в Тир-3.

### Бот v2 — детерминизация + планирование (новый трек, после баланс-прохода)
Зачем: текущий бот full-info + жадный 1-ply. Это (1) «читерит» (видит руку/колоду врага),
(2) слеп к навык-зависимым ключевикам (bypass/stealth/floodlight) → искажает баланс-сигнал.
Улучшение бота — самый высоко-leverage шаг для баланса (поднимает долю доверяемых карт).
- **П.1 Детерминизация — v1 СДЕЛАНО.** `Game::determinize(forSeat,rng)`: клон, у которого
  скрытые зоны оппонента (рука+колода) пересэмплены случайными картами из пула; свои не
  трогаются. Бот строит K=4 «мира», усредняет leaf-оценку (общие миры между кандидатами =
  CRN). Не угадывает верно — усредняет по гипотезам (робастная игра «вокруг» карт).
  - **Находка по сидениям:** честный бот даёт P1 **−9pp** (600 партий) против −4pp у
    читерящего → чит МАСКИРОВАЛ преимущество второго игрока. Структурный сигнал: компенсация
    P2 «+1 карта» (рука 4 vs 5) **переусиливает ~9pp**. Фикс — правило (P2 тоже 4 карты,
    или P1 «монета»/доп-кристалл), НЕ карты. Уточнить на бóльшем N с лучшим ботом.
  - **Важно:** детерминизация чинит ЧИТ (честная оценка темпа/карт-адвантажа/игры вокруг
    размена), но НЕ keyword-слепоту — bypass-слепота от ЖАДНОЙ мелкой оценки, не от знания
    руки. Её чинит только П.2. Т.е. для баланс-слепоты детерминизация сама по себе мало даёт.
  - **Минусы v1:** 4× медленнее (~1.4 партий/с, K=4 × JSON-clone); вера ГРУБАЯ (равномерно
    из всех 85, игнорит цвета/увиденное → воображает не-кастуемые офф-колор карты).
  - **Доработки:** (a) ускорить `clone()` нативным deep-copy — **СДЕЛАНО** (×16, не ×3–5:
    JSON-clone доминировал; честный бот K=4 теперь 21 партий/с, БЫСТРЕЕ старого читера);
    (b) обуславливать веру на цветах/сыгранном/лимите копий — TODO; (c) свой добор тоже
    скрыт (мелкий чит — знает свой порядок колоды в роллауте) — TODO.
  - **Сравнение честный vs читер (скрин 8000, журнал):** чит ЗАМЕТНО искажал карты, где
    важна скрытая инфа — дорогие бомбы и evasion. Сдвиги beta (честный−читер): `living_
    luminary` 9м −4.1→+0.5 (Δ+4.7: чит идеально играл вокруг бомбы), `ray_ram` bypass
    −2.8→−0.8 (наш отказ баффать подтверждён), `bloody_veil` +2.0→+0.05. → **прежние
    карточные выводы под читом для бомб/evasion недостоверны; честный скрин — новый базис.**
    Наш нерф `false_ripple` 3→4 держится (честный +0.97 resid +1.03, не выброс).
  - **Что детерминизация НЕ чинит:** дешёвые 1-дропы (`pollen` +7.1pp) — даже хуже. Это
    артефакт МУЛИГАНА (держит manaValue<4) + МАНА-САКА (жертвует дешёвейшее существо),
    не скрытой инфы. Открытый пункт: либо улучшить эти политики бота, либо исключать
    1-дропы из тюнинга (как keyword-слепые). Перекос сидений честным ботом −6.7pp
    (n=8000) против −4.2 у читера → компенсация P2 переусиливает ~2.5pp (правило, не карты).
  - **ПОТОЛОК per-card точности = шумовой пол бота ~2pp (ключевой вывод).** Confirm-скрин
    на боте с фиксом своей колоды (те же сиды, та же колода — изменился ТОЛЬКО бот) сдвинул
    βs многих карт на ±2pp (`teeming_host` Δ+2.4, `young_lightbloom` Δ+2.2, `luminary` Δ−2.0,
    `false_ripple` +1.0→+1.8). Значит при текущем качестве бота **дисбаланс ниже ~3–4pp НЕ
    измеряется устойчиво** — зависит от деталей политики. Робастен только КРУПНЫЙ сигнал:
    дешёвые 1-дропы (+5..7pp, но это bot-артефакт мулигана/мана-сака, не карты). Все mid-cost
    выбросы 2–3pp гуляют между версиями бота → тюнить их нельзя до усиления/стабилизации бота
    (PUCT). `false_ripple` через все боты стабильно +1..2.5pp над кривой → нерф 3→4 оправдан
    directionally, оставлен; остаток +1.8 не трогаем (бот-шум). **Итог баланс-арки: сет в
    целом честен, надёжных карт-правок нет кроме directionally-обоснованных; точный per-card
    тюнинг ждёт более сильного бота.**
  - **Чит своей колоды — ПОЧИНЕН.** `determinize` теперь мешает и СВОЮ колоду (игрок знает
    состав, не порядок) → роллауты не подсматривают свой добор. Чистый honesty-фикс.
- **Harness валидации силы — СДЕЛАНО** (`selfplay --vsgreedy`: seat1 играет жадным
  рефлексом `botGreedyAction`). Самоигра силу не показывает (оба — один бот); голова-в-голову
  показывает. seat0 = тестируемый бот (сидячий гандикап ~−5pp), `P1%` = его винрейт.
- **П.2 MCTS — ПОПРОБОВАН и ОТКАЧЕН (отрицательный результат, важный):** реализовал
  ансамблевый детерминизированный UCT (kWorlds миров × kIters итераций, лист = squash(evalState),
  минимакс через UCB, выбор по визитам). Поведение зависело от бюджета: kIters=200 → пассив,
  партии 53 хода, перекос +20pp (выбор почти случаен при ветвлении ~40); kIters=2000 →
  вменяемо (перекос −5pp, партии 19.6, короче 1-ply). НО голова-в-голову: **MCTS 38%
  против жадного = СЛАБЕЕ жадного**. А честный 1-ply — **47.7% ≈ жадный** (едва лучше).
  Вывод: рукотворный `score()` — сильная политика; наивный поиск над грубым evalState-листом
  её НЕ бьёт (MCTS усиливает ошибки листа). Сильный бот требует **PUCT** (вести MCTS приорами
  из `score()`, как AlphaZero) и/или **лучшего листа** — не «больше поиска». Откачено;
  оставлен зафиксированный честный 1-ply (≈ жадный, но честный за счёт детерминизации).
  Keyword-слепота поиском НЕ снята — ждёт PUCT/лучшего листа.
- **Чему научились (для будущего П.2):** (1) бюджет MCTS должен сильно превышать ветвление
  (иначе хуже жадного); (2) лист evalState слишком груб — нужен либо роллаут жадной политикой
  (медленно), либо PUCT-приоры; (3) ВСЕГДА валидировать силу головой-в-голову, не по «выглядит
  осмысленно» (короткие партии/вменяемый перекос обманули — бот всё равно был слабее).
- **Компромисс:** сильнее бот = резко медленнее селфплей. Баланс качества хода против объёма.
- **Честный предел:** один сильный бот ≠ человеческая мета (разнообразие архетипов,
  деко-сборка, ошибки). Цель — «не-читерящая, планирующая reference-политика», не идеал.

Покрытие свойств (срез 2026-06): `ambush`/`refract`=0 (нет в движке — реализовать или
убрать из `EFFECTS.md`), `add_crystal`=0 (дать зелёную ramp-карту), синглтоны
`flash`/`resonance`/`destroy` (часть by design), перекос `scry`/`provoke`=8, `pierce`=7,
`stealth`/`split`=6 — проредить.

## Параллельный трек P — полировка и game-feel (XL, фоновый)

- [ ] **Адаптивный UI** (пререквизит M5): stretch-режим/анкоры/минимумы/скроллы,
      проверка 16:9/16:10/маленькие окна, без обрезаний.
- [ ] **Звук** (обязателен до публики): SFX (розыгрыш/атака/урон/смерть/победа),
      эмбиент-музыка; шина громкости в настройках.
- [ ] **VFX каждому эффекту/ключевику**: (1) пассивный визуал состояния (лёд на
      замороженном) + (2) одноразовый в момент срабатывания (летящее ледяное копьё).
      Чистый код: GPUParticles2D/шейдеры/Tween. Сейчас состояния — только иконки.
- [ ] Game-feel: импакты (тряска+вспышка уже есть — расширить), частицы-искры,
      подъём-наклон карты под курсором.
- [ ] Анимированные герои: MJ Animate → loop → `.ogv`; VideoStreamPlayer в
      медальоне поверх png-фоллбэка; луп раз в ~15–30с.
- [ ] Косметика: layout-варнинг Godot при драге («non-equal opposite anchors…»,
      `_set_size`) — безобиден, headless не воспроизводится; найти узел, заменить
      `.size =` на `custom_minimum_size`/анкоры/`set_deferred`.

## Пост-релиз (решено делать, но после M-цепочки)

- **Мобильные браузеры/тач:** тач-управление (переосмыслить драг+дабл-тап: вероятно
  тап-выбор → тап-цель), портретная/компактная раскладка, крупные цели нажатия.
- **EN-локализация:** i18n-слой UI, EN-генератор текстов правил в `Glossary`,
  `name.en` к тому моменту уже в данных.

## Мелкие известные баги/долги (вне майлстоунов)

- `yellow_sunstroke`: генератор текста выводит `flash` как свойство «Вспышка N»,
  а накладываемый статус `blind` не поясняется — чинить в `Glossary`.
- `awaken` платит generic жадно (`genericPay` есть только в `play`) — докинуть
  выбор кристаллов и туда.
- Движок (менять только с тестами): awaken-скидка применяется лишь к `generic`
  (карта `generic:0` + цветные пипы скидки не получает — дизайн-вопрос);
  `buffStats` дублирует `recomputeContinuous` (safety net).
- Клиент: повторяющиеся `Label.new()`+оверрайды и StyleBox-литералы — дедупить
  лениво, при касании кода (целиком фазу «дизайн-токены» решили не делать).

## Журнал решений

- **2026-06: чистка языка эффектов — ортогональность.** Грамматика (`DESIGN.md` §8) —
  единственный источник правды; ключевик лишь именует точку грамматики. Введён тест
  «ключевик или инлайн» (континуальное / несомое существом / новый глагол — иначе инлайн-
  проза). **AoE = селектор, не действие:** добавлены селекторы `all_enemies` /
  `all_creatures`, `Game::selectTargets()`; действия-дубли `flash` и `damage_all`
  выпилены (теперь `blind`+`all_enemies` и `damage`+`all_creatures`), Нимб уважается
  единообразно. Мигрированы 5 эффектов в `cards/sample.json`+`client-godot/cards.json`,
  тесты движка и клиентский глоссарий. Текст карт генерируется из селектора. Тесты
  зелёные (движок 97, клиент 47). `glint`/`signal` как ключевики отвергнуты — это инлайн
  `on_play`+`damage`/`draw`.
- **2026-06: бот v1 + режим «Тренировка» (один бот).** Бот живёт в движке
  (`engine/src/bot.cpp`, `botNextAction`) — одна жадная эвристика поверх `legalActions()`
  (играй дёшево, меняйся в плюс, бей в лицо когда впереди по доске). Сам отвечает на
  мулиган/scry (их нет в legalActions). Возвращает action-JSON в формате клиента, так что
  «говорит» тем же протоколом. Сервер: `createBotRoom` (seat 1 = бот без сокета, fd=-1,
  `sendJson`/`broadcastRoom` гейтят fd<0), `pumpBot` доигрывает ход бота после хода человека
  → клиент видит ответ бота одной склейкой. Клиент: одна кнопка «Тренировка» в PlayMenu.
  Профили ботов (агро/контроль) и self-play CLI на этом же `botNextAction` — потом.
- **2026-06: токены не воидятся — scatter возвращает карту, смерть идёт в graveyard.**
  Бонс (scatter) любого существа возвращает его карту в руку; токен/иллюзия отдаёт карту,
  которую копирует (sprout → его 0-стоимостное тело; haunt/mirage-иллюзия → ОРИГИНАЛ карты,
  при переигрыше это уже настоящее тело, не 1 HP). Смерть токена тоже кладётся в graveyard
  (счётчик растёт и от токенов). Токен в руке/на столе не несёт триггеров родителя (`token`
  гейтит haunt/split/spores). UI-долг: иллюзии на борде ничем не помечены — пометить при
  качественном UI-проходе.
- **2026-06: decoy подразумевает awaken-способность.** Карта с `decoy` будится из
  мана-ряда даже без ключевика `awaken` (раньше 5-цветные `prismatic_*` с decoy без
  awaken вообще нельзя было разбудить — decoy был мёртв). `delay` теперь запоминает
  выбранную цель и карту-источник, так что отложенный таргет-эффект (урон по
  выбранному существу) попадает по нему через N ходов, а не физзлит по target=0.
- **2026-06: контент-чеки в CI** (job `checks`, только python, без сборки): `tools/
  check_ids.py` (id == цвета+snake(en)) и `tools/check_art.py` (у каждого `client-godot/
  art/*.png.import` есть сайдкар и `mipmaps/generate=true`). Второй ловит регрессию
  «арты зернят в игре»: дефолтный импорт Godot ставит mipmaps=false, и свежие арты
  молча теряют их, а `Tokens.art` сэмплит мипы (`*_WITH_MIPMAPS`) — без них алиасинг.
- **2026-06: цель — публичный web-релиз**; роадмап этого файла заменяет прежние
  приоритеты. Рефакторинг клиента (ARCHITECTURE/чистка/тесты/декомпозиция Main)
  завершён; стретч «типизация+дизайн-токены» отменён осознанно.
- **2026-06: карты — без редкости и без коллекции, все доступны всем.** Редкость и
  азарт лутбоксов — только на КОСМЕТИКЕ (рамки/борды/скины), геймплей весь бесплатен
  → ноль pay-to-win, проще бэкенд (нет коллекции на аккаунт). `rarity` в `CardDef`
  не добавляем (задача снята с M1). `name.en` — конвенция контент-трека.
- **UI через Claude Design** (трек P): экраны клиента проектируются в claude.ai/design
  (prompt-to-prototype). **Есть постоянный проект Prism**, где уже живут борд, меню и
  лобби — визуальный язык и компоненты (стеклянные панели, чипы, кнопки, цвета карт,
  свечения) там уже заданы. Поэтому бриф **короткий и ссылочный**: «в этом же проекте,
  переиспользуя визуальный язык и компоненты борда и меню/лобби, добавь экран X…» —
  айдентику (цвета/шрифты/токены) НЕ переописываем, инструмент её знает. В брифе:
  формат **1600×900**, **по-русски**, **без эмодзи** (только текст + inline-SVG), затем
  конкретное содержание экрана и поведение. Цикл: Claude Code пишет бриф → пользователь
  мокапит в claude.ai/design → отдаёт результат → Claude Code реализует в Godot. Вывод
  инструмента — веб-спека вида, не drop-in для Godot (эталон, по которому кодим).
- **2026-06: валидация колод — в M4, не M1.** Делаем вместе с колодостроителем и
  пресетами (иначе негде взять легальную 40/≤2 колоду до билдера). `demoDeck` («1
  копия каждой») остаётся для теста всего сета; в M4 станет dev-only песочницей с
  байпасом валидации. M1 = legalActions + сериализация + реплеи + фазз (закрыт).
- **2026-06: рамки релиза (решено):** (1) **десктоп-браузеры сначала**, мобилки/тач —
  отдельный пост-релизный этап (тач-управление переосмысляется: сейчас драг+дабл-тап);
  (2) **язык релиза — RU**, EN-локализация (генератор правил + UI) — после релиза,
  `name.en` копим в данных с контент-батчей; (3) **экономика — только игровая
  валюта**, реальных денег нет → аккаунты остаются лёгкими (токен), платёжной
  инфры и юр-рисков лутбоксов нет; (4) **VPS/домена пока нет** — подбор (бюджет
  **деплой готов, но на паузе** — turnkey-стек (`deploy/`, Docker+caddy) собран и
      проверен в контейнерах; live-разворот ждёт окончания «июньской блокировки» ТСПУ
      (бьёт по всем хостерам разом, не чинится выбором провайдера); поднимем за ~10
      мин, когда РФ-связность стабилизируется.
- **Баланс сета (сетка, чтобы тюнить от единой базы).** Ванильная стат-линия
  существа: `ATK+HP ≈ 2·T + 1`, где `T` = полная стоимость. Ключевики выкупаются
  из этих очков (≈: pierce/ward/regen/self_lifesteal/growth ≈ 1.5; shield/bypass/
  photosynthesis ≈ 2; provoke/lingering/stealth/haunt/compost ≈ 1; split/germinate/
  decoy ≈ 1.5). Цветные пипы — лёгкая компенсация (~+1). Кривая T1–T8, бугор T3–4;
  провок-стены — высокий HP/низкий ATK; фотосинтез — временная мана; `damage_all`
  бьёт и свою доску. Пятицветки — аспирационный верх. Скорер: `tools/balance.py`.
- **Резонанс — снапшот при выходе** (не непрерывный), иначе снежный ком в лейте.
- **Силы героев — пассивные, не кнопки**: постоянная черта малой магнитуды; герой =
  `CardDef type:hero`, не в колоде; once-per-turn — счётчик `heroPowerUses`. Урон
  герою — всплывающая `-N` у медальона.
- **4 героя (≈равной силы):** Ирида «Призма»/`spectral_shift`; Эреб «Затмение»/
  `lighteater`; Тициана «Палитра»/`palette`; Гемма «Огранка»/`facet`. Единый стиль
  «живого света» (Эреб — ч/б). Кандидат на возрождение: Кьяра «Линза» (`hero_lens.png`
  сохранён). Детали — `ART_HEROES.md`.
- **Схема стола — «фланговые медальоны»**: герой слева, стол в центре, стопки справа.
- **Арты: герои 1:1 (1024²), карты 2:3.** Стиль — «существа из живого света»
  (`ART.md`); **имя и арт изображают один субъект**.
- **Жесты:** драг = разыграть/атаковать, **двойной тап = в ману**; цель — только
  перетаскиванием (клик-таргетинг вырезан).
- **Лицо карты:** арт во весь кадр + ободок + статусы; имя/правила — в тултипе.
- **Временная мана видна** (пипсы по `max(crystals, available)`); выбор цвета
  банкуемой мультицветки — радиальный пикер; выбор кристаллов на generic-часть —
  тап-пикер (`ManaSpendPicker`), движок принимает `genericPay`.
- **Freeze vs Blind:** заморозка — не атакует, но отвечает; ослепление — не наносит
  боевой урон вообще.
- **Морок (haunt):** иллюзия-токен наследует боевые ключевики, но не перерождается
  и не сыплет споры (`token` гейтит триггеры).
- **Сброс — пока информационный** (только размер наружу); задел под реанимацию.
- **DEF убрали:** ATK/HP, взаимный боевой урон (как HS).
- **Равенство типов сета:** моно поровну (3/цвет), двуцветки поровну (1/пара),
  нейтраль (5) и пятицветки (5, префикс `prismatic_`) — отдельные пулы.
- **Scry не меняет счётчик колоды** (peek-карты числятся в колоде).
- Позиционная боёвка — **НЕ делаем**.

## Открытые вопросы / идеи

- **Ось различия — ГЕРОЙ, а не колода** (кандидат): общая/пресетная колода, глубина
  в герое с китом способностей. Не реализуем — смена идентичности игры; вернуться
  при дизайне M7-режимов.
- **Архетип «бей по своим»** — движок готов (селекторы friendly/any); чистый контент.
- **«Мана — контестед-ресурс»** — нужны ключевики атаки по мане/перекраски; отложено.
- **Плагины (как HS Deck Tracker):** read-only API состояния матча + песочница;
  после M6 (нужны стабильный протокол и аккаунты).
- **Lua-«аварийный люк»** (sol2) для экзотики, если грамматики не хватит.

## Вырезанные ключевики (кандидаты на возврат в треке К)

- **🔴** `penetrate` (игнор брони/щитов), `deepwound` (−N HP навсегда — дубль lingering).
- **🟡** `reveal` (дубль floodlight), `exposed` (нельзя спрятать + урон), `glare`
  (ранивший ослепляется — 3-й вариант ослепления).
- **🟢** `seed` (дубль spores), `proliferate` (токен каждый ход), `bloom` (дубль
  resonance), `choke` (вражеский кристалл → твой росток).
- **🔵** `brittle` (двойной урон по замороженному — оно и так нейтрализовано),
  `mass_freeze`, `sleep`, `insomnia` (аура «враг не лечится»), `insight` (видна рука).
- **🟣** `phantom` (дубль stealth), `corrupt` (−N/−N, слабо «фиолетовый»).
- **Концепты:** мимикрия, двойник/клон, полиморф; огненная ветка Красного заменена
  на «красный свет».

## Публичная страница разработки (GitHub Pages)

Витрина «над чем идёт работа» для друзей/зрителей. Источник данных — `roadmap.json`
(курируемые треки и фазы) + `git log` (все коммиты). Генератор `tools/build_roadmap.py`
собирает единственный выход `docs/index.html` (не править руками): hero-wordmark «PRISM»
(шрифт игры `ChakraPetch-Bold.ttf` в `docs/fonts/`, спектр — в ауре и лучах, не на
буквах) + карта на **оси времени**. Слой коммитов: каждый коммит — точка на луче своего
трека (привязка по файлам, `track_of_path`; fallback → Инфра, так что коммит не
теряется), зум разводит плотные кластеры, hover → хеш+дата+сабджект. Слой фаз: этапы из
`roadmap.json`, done/active распределены по периоду трека, planned — в зоне «план» правее
линии «сейчас», abandoned — крестик. Зум/пан через `viewBox`
(колесо/перетаскивание/двойной клик-сброс).

Автодеплой: `.github/workflows/pages.yml` на каждый push в `main` гоняет
`build_roadmap.py` с `fetch-depth: 0` и публикует `docs/` через `deploy-pages` — без
коммитов обратно, всегда свежий лог. Включение (один раз): Settings → Pages → Source:
**GitHub Actions**. URL `temikgo.github.io/prism`. Новый трек = добавить руками в
`roadmap.json` (имя+hue) и в `track_of_path` (правило пути).
**Ведение:** слой коммитов автоматический, слой фаз — редакторский. Дойдя до значимого
этапа, перед коммитом обновить `roadmap.json` (active→done, добавить узел) — Actions сам
этап не выведет, только точки-коммиты.

Открыто: фазы распределены по периоду трека, не привязаны к конкретным коммитам (точки —
точные); при желании — проставить дату каждой фазе в `roadmap.json`.

## Дев-инструменты (текущие)

Скриншот-стенды: `Shot.gd` (мок-борд через `DevKit.demo_view()` → `_shot.png`),
`ShotMenu.gd` (экраны оболочки). Тесты клиента: `tests/run.gd` (headless, 40).
Харнес моков: `devkit.gd` (валидация id против `cards.json`). Запуск — шпаргалка
в `ARCHITECTURE.md A8`.
