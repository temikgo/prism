# Prism — backlog и рабочий план

> **Как вести этот файл (читать первым после компакта).** Это единственный источник
> правды по «что делаем и что решили». Правила: (1) **самодостаточность** — никаких
> ссылок на чат/память, всё нужное пишем сюда; (2) активная работа — в «Сейчас в работе»
> явными чекбокс-задачами с критерием готовности; (3) каждое принятое дизайн-решение
> фиксируем в «Журнал решений» одной строкой, чтобы не потерять при компакте.

## Сейчас в работе

**ПРИОРИТЕТЫ (решено с пользователем).** Сейчас строим техническую основу, баланс — позже.
Порядок: **(1) UI game-feel / VFX → (2) дека-модель + удобство сети → (3) геймплей-баланс +
баг haunt**. Обоснование пользователя: баланс — долгая полировка игры, можно отложить; сейчас
важнее техоснова и «ощущение». Движок остаётся **Godot 4** (тянет уровень индустрии; менять не
надо — иначе выкинем рабочий C++-движок и клиент).

- [ ] **Фаза game-feel (сейчас):** анимации (карты летят/ложатся, атака с рывком, вход существа),
      подъём-наклон под курсором, импакт (тряска экрана + вспышка при уроне), частицы-мошки/
      искры, шейдер-свечение готового к атаке. Чистый код (Tween/AnimationPlayer/GPUParticles2D/
      шейдеры), без новых ассетов. **Замечание про архитектуру:** клиент сейчас полностью
      пересобирает борд на каждый `view` (`_rebuild`), поэтому межсостоянные анимации (карта летит
      рука→стол) требуют либо переживающих ребилд узлов, либо анимации входа внутри ребилда. Начали
      с того, что работает с ребилдом: эмбиент-частицы, вход-анимация карт, импакт-тряска, ховер.
- [ ] **Дека-модель + сеть (потом):** настоящие колоды (лимиты копий, кривая) вместо `demoDeck`;
      удобство сетевой игры (комнаты по коду, реконнект).
- [ ] **Баланс + haunt (отложено):** базовый баланс-проход + фикс haunt-рекурсии (см. ниже
      «Журнал решений → ИЗВЕСТНЫЙ БАГ»).

**Скриншот-стенд для разработки UI:** `client-godot/Shot.gd` — рендерит мок-борд (без сервера) в
`_shot.png`. Запуск: `Godot --path . -s Shot.gd`. Позволяет видеть UI-правки сразу. Dev-only.

**UI / взаимодействие:**
- [ ] **Лог действий (история хода).** Нужна видимая лента того, что произошло: кто что разыграл,
      атаки, смерти, эффекты, добор/мана, срабатывания пассивов героя. Сейчас на борде видно только
      результат (новый `view`), но не «что именно случилось» — особенно теряются эффекты соперника
      и цепочки (споры/haunt/компост, заклинания). Варианты: серверные событие-сообщения (расширить
      протокол) либо клиентский дифф view→текст. Полезно и для отладки, и для игрока. Решить объём
      при реализации; пока — пункт в очередь.
- [x] **Клик-таргетинг убран — цель выбирается ТОЛЬКО перетаскиванием.** Вырезан весь клик-флоу
      «выбери-потом-цель»: атака существом, каст таргет-заклинания, awaken-таргет. Удалены
      `_on_hand_card`/`_on_my_creature`/`_on_enemy_creature`/`_on_enemy_hero`, состояние
      `attacker_id`/`casting_index`/`awaken_index`/`pending_side`, `_clear_selection`,
      `_attacker_has_bypass`, `UiCard.targeting`, подсветки целей по этим флагам. **Оставлено:**
      драг (играть/прицел/атака), двойной тап = в ману, **одиночный клик по awaken-чипу для
      НЕтаргетного существа** (выходит на свой борд — это логично, не клик-таргетинг).

**Оболочка приложения (меню/экраны/комнаты) — дизайн в `APP_SHELL.md` (читать первым).** Решено:
**без имён/аккаунтов**; матчмейкинг — **коды комнат с обязательным паролем**; + кнопка-заглушка
**«Поиск»** (будущий автоматчмейкинг). Экраны под роутером: MainMenu / PlayMenu (Создать/Войти/
Поиск) / CreateRoom / JoinRoom / RoomWait / Match (нынешний борд) / Result / Settings. **Сейчас все
играют одной общей колодой** — экран выбора колоды не делаем (он по режиму, на будущее). Фазы:
A — скелет оболочки (клиент, без сервера; «Играть» → прямой connect временно), B — комнаты
(сервер RoomManager + лобби-протокол + пароль), C — выбор колоды/героя (с дека-моделью). Подробно —
`APP_SHELL.md`.

**Фаза A — СДЕЛАНО (клиент).** Корень — `App.tscn` → `Router` (router.gd): держит экраны и
сохранённый адрес сервера (`user://settings.cfg`), переключает MainMenu ↔ Settings ↔ Match.
Новые модули: `backdrop.gd` (общий фон — градиент/свечения/виньетка/мошки, вынесен из `Main.gd`),
`main_menu.gd` (заголовок PRISM + призма-полоса + Играть/Колоды(заглушка)/Настройки/Выход),
`settings_screen.gd` (только адрес сервера, без имени). `Main.gd` стал экраном Match: убран
connect-бар (адрес приходит из Settings через `start(url)`), вместо него слим-бар «В меню» + статус;
сигнал `exit_to_menu` (game-over и кнопка «В меню» → роутер). «Играть» пока ведёт в прямой connect
к адресу из Settings (комнаты — Фаза B). Проверено скриншотами (`ShotMenu.gd`) + парс всего проекта.

**Фаза B — комнаты (сервер + клиент) — СДЕЛАНО.** Сервер (`server/src/main.cpp`) переписан из
«один матч на процесс» в **event-loop менеджер комнат**: сокеты ведутся через лобби, команды
`createRoom`/`joinRoom`/`leaveRoom` + пароль, серверные коды (4 символа из однозначного алфавита),
ответы `roomCreated`/`joinError{no_room|bad_password|room_full}`/`matchStart`/`opponentLeft`, много
комнат на процесс, чистка при дисконнектах. Клиент: `Router` теперь сетевой хаб — владеет `Net`,
роутит лобби-ответы (есть `type`) vs. view (нет `type`); экраны `play_menu`/`create_room`/
`join_room`/`room_wait`; `Main` стал экраном матча через инъекцию (`bind(sender)` + `feed_view`),
кнопка «В меню» вынесена на всегда-верхний слой `_topbar` (кликабельна и поверх мулигана/гейм-овера),
статус-сообщения — янтарная стеклянная плашка. **Reconnect после рестарта сервера** (при обрыве
`Net` освобождается, отправка переустанавливает связь). «Играть» теперь ведёт в `PlayMenu`, а не в
прямой connect. Проверено: 10 серверных edge-кейсов (Python) + e2e через настоящий клиент
(создать→войти→матч), переподключение, рестарт сервера, сервер недоступен. **Осталось глазами:**
во время мулигана/гейм-овера выход; «соперник вышел» / «соединение потеряно» — отображение плашки.

**Типографика + кнопки — СДЕЛАНО (ждёт проверки).** Шрифты в `client-godot/fonts/`: **Lato**
(полная кириллица) — весь UI; **Dela Gothic One** (тяжёлый дисплейный, есть кириллица) — логотип
PRISM и крупные титулы. Модуль `fonts.gd` (`Fonts`: REGULAR/SEMIBOLD/BOLD/BLACK/DISPLAY +
`default_theme()`); тема выставлена на корне роутера и в `Main` (наследуется всеми экранами и
скриншот-стендом). `Ui.neon_button` теперь Lato Semibold; убран прямоугольник фокуса
(`focus_mode = NONE` + пустой `focus`-stylebox — это и был «прямоугольный слой при нажатии»).
`Ui.label` получил флаг `bold` (Lato Semibold). Логотип — Dela Gothic One; «Настройки» — Lato Black;
«ПОБЕДА/ПОРАЖЕНИЕ» — Dela Gothic One.

**Новый ростер из 4 героев — СДЕЛАНО (ждёт проверки).** Цель: 4 разных героя примерно равной силы.
Реализованы пассивы (движок + глоссарий + иконки + тесты):
- **Ирида «Призма»** — `spectral_shift` (без изменений).
- **Эреб «Затмение»** — `lighteater` (вместо `umbra`): существо, нанёсшее боевой урон твоему герою,
  навсегда теряет 1 атаки. Истощение/контроль.
- **Тициана «Палитра»** (новый, `hero_palette`) — `palette`: первый за ход розыгрыш карты со
  стоимостью из 2+ цветов даёт добор. Value-движок.
- **Гемма «Огранка»** (новый, `hero_facet`) — `facet`: можно будить (awaken) ЛЮБУЮ банкованную
  карту, разбуженные существа выходят с +1/+1. Рамп/value вокруг банка.
Общий флаг once-per-turn-пассивов заменён на счётчик `Player.heroPowerUses` (лимит у каждого свой;
во view отдаётся `heroPowerUsed`). Арты: `hero_palette.png`/`hero_facet.png` готовы; Призма/Эреб —
старый арт. Иконки `palette.svg`/`facet.svg` созданы.
**Мёртвый код старых пассивов вычищен:** `umbra` и `clairvoyance` убраны из движка/протокола/
глоссария/иконок/Main, их тест-дефы и тесты удалены, `lens.svg` удалён.
**Отложено (кандидат):** Кьяра «Линза» / `Прозрение` (`clairvoyance`) — арт `hero_lens.png` сохранён,
для возрождения заново подключить топ-карту во view + иконку (см. `ART_HEROES.md`).

- [ ] **Промпт на генерацию логотипа + надписи и элементов игры.** Собрать арт-промпт(ы) для:
      (1) **логотип/wordmark «PRISM»** (призма, расщепляющая луч в спектр; на тёмном; под наш стиль
      «свет на тьме»; варианты — иконка-печать и горизонтальный wordmark), (2) **фоновые/декор-
      элементы оболочки** (рамки панелей, разделители, угловые орнаменты, кнопочные «глассы»),
      (3) иконки-заглушки разделов (Колоды, Поиск). Формат как `ART_CARDS.md`: BASE-стиль + по
      элементу Subject + путь сохранения (`client-godot/art/ui/<name>.png`). Арт генерит пользователь,
      я только собираю промпты и пути. **Сейчас логотип — текст Dela Gothic One**, промпт даст
      графическую замену позже.

**Рефакторинг клиента (Godot) — план в `REFACTOR.md`.** `Main.gd` — god-object (2415 строк,
~100 функций, 5+ ответственностей: сеть/состояние/анимации/правила/UI; 43 функции читают глобалы;
дубли `_gem`/`_stat_gem`, `_art_thumb`/`_art_full`, стиль-фабрики; 0 клиентских тестов). План —
поэтапно снизу вверх (чистые переносы, проверка по `Shot.gd`): Ф1 `CardData` → Ф2 `Tokens`/фабрики
→ Ф3 виджеты в `class_name` → Ф4 `Net`/`GameState` → Ф5 `Fx`. Отдельно потом: инкрементальный
апдейт борда (вместо полного `_rebuild`) и типизированная модель. Подробно и с обоснованием —
`REFACTOR.md` (читать перед началом).

**Геймплей/движок — в очередь:**
- [x] **Призыв новых существ кладёт их криво по позиции — ПОЧИНЕНО.** `summonToken` получил
      параметр позиции вставки (вместо `push_back` в конец). Событие `Died` несёт слот, где стояло
      существо, поэтому споры/haunt появляются на месте гибели; `split`-копии встают вплотную к
      родителю; `germinate`-росток — рядом с пророщенным. **Приоритет при смерти (решено с
      пользователем):** компост (по доживших) → **haunt-копия** (первой, держит слот при полном
      борде) → споры (справа от копии). Мираж — осознанно остаётся append (целевое заклинание).
      Тесты: обновлён split-тест + 2 регрессии (позиция от смерти; spores+haunt — копия раньше
      ростков). Клиент не трогали (рисует борд по порядку массива). 69 тестов зелёные.

**Глоссарий / генерация текста правил — в очередь:**
- [ ] **Заклинание Вспышки: показывается свойство «Вспышка», но не поясняется «ослепление».**
      У `Озаряющая Вспышка` эффект `flash` инлайновый (не печатный ключевик), но генератор текста
      (`_build_tooltip` + `Glossary`) выводит его как свойство «Вспышка N: …» (т.к. `flash` есть в
      `Glossary.KW`). При этом статус, который он накладывает — **ослепление** (`blind`) — не
      поясняется. Надо: не выводить `flash` как отдельное свойство, а пояснять результирующий
      статус «ослепление» (как уже делается для freeze/blind-заклинаний). Это логика текста
      правил, не визуал.

**Сделано в UI-проходе (Фазы 1–2 + борд):** лицо карты — арт во весь кадр + тонкий цветной
ободок + скримы + статусы `+N`, без текста на лице (имя/правила — компактный ТЕКСТОВЫЙ тултип,
зум-арта НЕТ — отверг пользователь). Карты крупнее (176×246). Атмосфера: свет Мега-Призмы сверху,
фиолетовый отлив снизу, виньетка. Баннер хода — подсвеченная плашка. Ауры/awaken-чипы — арт-онли
(без переноса имён). Центр-линия убрана (выглядела криво).

**Старый план UI по фазам — в `UI_DESIGN.md`**

**Ранее фокус: ПЕРЕДЕЛКА ИНТЕРФЕЙСА под арты — дизайн зафиксирован в `UI_DESIGN.md`
(читать первым в UI-фазе).** Пользователь доверил дизайн; ключевые требования: стильно, в
стиле «свет на тёмном», очень удобно, и БЕЗ наложений/вылезающего текста (см. инварианты §1).
Реализация по фазам (§10): 1) каркас лица карты + состояния-через-свет, 2) зум-попап + рука,
3) атмосфера стола, 4) герой, 5) стрелка/анимации/типошкала. Старт — по команде пользователя.

**Предыдущий фокус: новый сет карт — световые концепты + баланс. СДЕЛАНО (ждёт проверки).**

- [x] Арт-стиль **решён и зафиксирован**: «существа из живого света» (см. «Журнал решений»);
      `ART.md` и `ART_HEROES.md` переписаны под него.
- [x] **Новый сбалансированный сет (`cards/sample.json`, синхрон в `client-godot/cards.json`).**
      Старые 27 удалены. Структура с **равными типами** (требование пользователя):
      **15 моно (3×каждый цвет, поровну) + 10 двуцветок (по 1 на каждую из 10 пар, поровну)
      + 5 нейтральных (белый утил-пул) + 5 пятицветок + 3 героя = 38**. Используются **все
      реализованные механики** (37 шт. — проверено скриптом). Есть **добор** (Хрустальный Разлом,
      Светлый Дар) и **генерация маны** (фотосинтез: Светоросток, Призменный Накопитель/белый,
      Солнечный Оплот, Ледяная Поросль). Балансная сетка — см. «Журнал решений → Баланс».
- [ ] **Проверка пользователем в игре** (не коммитим до «пуш»).
- [ ] Дека-модель: настоящая колода вместо `demoDeck` (см. ниже про текущую генерацию).
- [ ] Перегенерить арты под новый сет (поле `art` каждой карты — уже готовый световой SUBJECT).

**Покрытие механик новым сетом (карта → ключевик):** 🔴 pierce(Алое Жало, Призма-Таран),
self_lifesteal(Тлеющий Ловчий, Терновый Жнец), regen(Тлеющий Ловчий, Серафим), bypass+lingering
(Неугасимая Комета), lingering(Алый Лазутчик). 🟡 provoke(Маяк-Страж, Солнечный Оплот, Серафим),
shield(Зеркальный Нимб, Призма-Таран), ward(Зеркальный Нимб, Серафим), floodlight(Дозорный Луч),
blind(Слепящий Иней), flash(Озаряющая Вспышка). 🟢 photosynthesis(Светоросток, Солнечный Оплот,
Ледяная Поросль, Призменный Накопитель), germinate+spores(Лесная Праматерь), growth+undergrowth
(Древо Изобилия), resonance(Терновый Жнец), compost+spores+haunt(Споровый Дух). 🔵 freeze(Стылый
Росчерк, Стужный Пробой, Слепящий Иней), dispel+draw(Хрустальный Разлом), delay+dispel+scry
(Глубинный Оракул), chill(Ледяная Поросль), scatter(Зыбкое Смещение). 🟣 stealth+split(Сумрачный
Лазутчик), awaken+decoy+haunt(Терпеливый Морок), mirage(Призрачный Двойник), stealth+lingering
(Багряная Тень). ⚪ damage_all(Спектральный Залп), destroy(Развоплощение), draw(Светлый Дар),
photosynthesis(Призменный Накопитель), ваниль(Гранёный Страж). Герои: spectral_shift/umbra/clairvoyance.

**Текущая генерация колоды (что меняем):** `server/src/main.cpp::demoDeck` — фикс. демо: берёт все
не-геройские карты по порядку и набивает 30 слотов по кругу (`deck[i]=ids[i%size]`, обобщённо по
числу карт), оба игрока получают одинаковую колоду, дальше `Game.start()` тасует. Нет лимитов
копий, ролей, выбора — это следующая задача (дека-модель).

**Сделано (закоммичено): UI/UX-рефактор клиента — «фланговые медальоны» + полный пасс стиля.**

- [x] Новая раскладка `_rebuild`: каждая половина = HBox `[медальон героя | стол-центр | стопки]` (`_player_half`).
- [x] **Медальон героя** (`_hero_medallion`): крупный портрет 124px, HP/броня гемами на нём, имя, бейдж пассива; вражеский — дроп-цель атаки в лицо.
- [x] **Колонка стопок** (`_piles_column`): колода/сброс как визуальные стопки (`_pile_stack`), мана-блок с пипсами (`_mana_pips`, HFlowContainer-перенос), мана-ряд, верх колоды (Линза), счётчики/отложенные.
- [x] **Пасс стиля/локализация**: баннер/контролы/подсказки/«В МАНУ»/коннект-бар/ВЫ-СОПЕРНИК — на русском.
- [x] Удалены `_enemy_strip`/`_me_strip`/`_hero_block`/`_counts_label`/`_mana_crystals`/`_hero_style`.
- [x] Клиент парсится и грузится без ошибок.
- [x] Пост-правки 1: в ману — **двойной тап** (зона «В МАНУ» убрана, клик не играет существо — драгом); стрелка прицела очищена (убран нижний слой-тень); тултип поясняет статус-эффекты без ключевика; перенос существ на столе по ширине.
- [x] Пост-правки 2: двойной тап через **нативный `double_click`** (надёжно на ПК); пикер цвета поверх всего (`z_index=4096`); **прожектор/мана-ряд в ограниченном по высоте скролле** компактными миниатюрами — больше не раздувает колонку и не прячет второй борд.
- [x] Визуальная проверка пользователем — ок («работает великолепно»).
- [x] Коммит UI-рефактора + героев + 5-цветной карты + радиального пикера.

**Сделано ранее: E — герои + силы героя.** Модель и 3 героя реализованы (см. «Журнал решений»).

- [x] Движок: `type:hero`, `Player.hero`, рандом-пик на старте (сервер), пассив = ключевик.
- [x] Пассивы: `spectral_shift` (оплата), `umbra` (урон по герою, вкл. pierce-overflow), `clairvoyance` (view).
- [x] Протокол/клиент: герой публичен, бейдж пассива + крупный портрет, чип верха колоды для Линзы.
- [x] Тесты (doctest) на все три пассива + pierce∩umbra; clang-format; клиент парсится.
- [x] **Арты-портреты**: 3 PNG в `client-godot/art/hero_{prism,eclipse,lens}.png` — готовы (будут перегенерированы при упрощении стиля).
- [ ] (опц.) Ростер >3: кандидаты-пассивы в `EFFECTS.md` («Заря»/`dawnlight`, «Гало»/`halo`, «Полный спектр»/`spectrum`).
- [ ] Экран выбора героя — отложено до D (лобби).

## Журнал решений

- **Баланс нового сета (сетка, чтобы тюнить от единой базы).** Ванильная стат-линия
  существа: суммарные очки `ATK+HP ≈ 2·T + 1`, где `T` = полная стоимость (generic + цветные
  пипы). Ключевики **выкупаются из этих очков** (примерно: pierce/ward/regen/self_lifesteal/
  growth ≈ 1.5; shield/bypass/photosynthesis ≈ 2; provoke/lingering/stealth/haunt/compost ≈ 1;
  split/germinate/decoy ≈ 1.5). Цветные пипы дают лёгкую компенсацию (карта может быть на ~1
  очко выше ваниля за требовательность цвета). Кривая: моно — низ/середина/верх (T2/T3/T5–6),
  двуцветки кучнее в середине (T3–4), нейтраль — утилита. Герой 30 HP. **Числа — стартовые,
  тюним по игре.** Заметки по конкретным картам: provoke-стены идут с высоким HP/низким ATK;
  фотосинтез даёт **временную** ману (per-turn, не копится → не снежный ком); `damage_all`
  бьёт и свою доску (симметрично). Файл сета: `cards/sample.json`.
- **Ревью-пасс по сету (баланс/имена/кривая).** Кривая закрывает **T1–T8**, бугор на T3–4;
  добавлены 1-дропы (Алое Жало 2/1 pierce за {red}; Маяк-Страж 1/3 provoke за {yellow}), иначе
  1-й ход был «только в ману». Стат-нюджи: Тлеющий Ловчий 3/3→2/3 (двойной сустейн был силён),
  Багряная Тень 3/3→4/3 (двуцветка-ассасин была слаба), Стужный Пробой урон 2→3. Все 35 имён
  уникальны (дедуп: были по два «Зеркальный/Лазутчик/Морок», три «Призменный»; осталось общее
  «Страж» ×2 — ок). На заметку: Призма-Таран 4/3 shield+pierce за T3 — на грани (оправдано
  налогом на два цвета: R+Y к 3-му ходу собрать трудно); T5 и верх (7–8) намеренно тонкие —
  кастуемый топ это Оракул/Комета/Древо, пятицветки — аспирационный верх. **Числа стартовые.**
- **Резонанс — теперь снапшот при выходе** (движок). Был непрерывным (`recomputeContinuous`:
  ATK = base + resonance·кристаллы каждый пересчёт) → снежный ком, в лейте тело росло до 12/12.
  Стало: при призыве один раз впечатывается `resonance·кристаллы` в base ATK и в HP (как HP-часть
  undergrowth), дальше не растёт. Резонанс убран из непрерывного слоя. Тест на снапшот добавлен.
- **Пятицветки удешевлены и усилены** (были «очень слабые» при требовании всех 5 цветов):
  Серафим {g2}7/7→{g1}8/9; Левиафан {g3}9/8 pierce→{g2}11/10 pierce+bypass; Феникс {g1}5/5→{g0}6/6;
  Морок/Мираж {g1}4/4→{g0}6/6; Сверхновая {g3} dmg_all4→{g2} dmg_all6. Они должны быть бомбой-наградой.
- **Споровый Дух** ужат: 2/4 spores2 → 2/3 spores1 (временный нерф; корень — ниже).
- **ИЗВЕСТНЫЙ БАГ (отложен по решению): haunt-рекурсия / бесконечный самопризыв.** haunt оставляет
  1-HP копию, копия наследует ключевики → у неё снова haunt → ∞ (и spores сыплет ростки на каждой
  смерти) — существо фактически неубиваемо. **Согласованный фикс (делать в фазе баланса/UI):
  «Иллюзия» — видимый тип**: копия наследует только боевые/статические ключевики, а триггеры
  (haunt/spores/on-play/при-смерти) у неё отсутствуют и **скрыты на карте** + бейдж «Иллюзия»
  (прозрачно, что сработает; рекурсии нет). Технически: у `Creature` уже есть флаг `token`;
  гейтить триггеры в `reactTo`/`resolveOnPlay` по нему (нужно протащить `token` в `Event`), а в
  клиенте прятать триггер-ключевики у токенов и рисовать бейдж. Текущий нерф spores=1 — временно.
- **Равенство типов (требование пользователя):** моно поровну (3 на цвет), двуцветки поровну
  (1 на каждую из 10 пар). Нейтраль (5) и пятицветки (5) — отдельные пулы, под равенство не
  попадают (белый утил + флагман-показ радиального пикера). **Пятицветки носят общий id-префикс
  `prismatic_`** (как у моно/пар/нейтрали есть свой префикс), отображаемые имена — разные.
- **Силы героя — пассивные, не кнопки** (ушли от HS-активки-за-ману): постоянная черта,
  гнущая одно правило; нарочно условные и малой магнитуды. Реализация: герой = `CardDef`
  `type:hero`, пассив = именованный ключевик; герой не в колоде, рандом на старте (сервер).
- **Лор героев:** художники Призма-Арены. Отображаемое имя — **одно слово** (Ирида/Эреб/
  Кьяра); мантия (Призма/Затмение/Линза) — лор, не часть имени.
- **Стартовая тройка:** Ирида/`spectral_shift` (раз в ход кристалл как соседний цвет),
  Эреб/`umbra` (−1 урон по герою, ловит и pierce), Кьяра/`clairvoyance` (виден верх колоды).
- **umbra ловит pierce-overflow** (общий хелпер `heroHitDamage`), иначе pierce «проходил сквозь».
- **Схема стола — «фланговые медальоны»** (а не центр-герой HS): герой крупным портретом на
  левом фланге половины, колода/сброс/мана стопками на правом, стол в центре. Полный пасс стиля.
- **Арты героев — квадрат 1:1** (`1024×1024`), клиент кадрирует квадратом; карты — портрет 2:3.
- **Арт-стиль — «существа из живого света».** Душа арта = свет/спектр, он **суть, а не косметика**.
  Не заимствуем дженерик-стили (HS/Clash/Valorant поверх обычных субъектов → бездушно; «мультяшное»
  отвергнуто). Стиль = силуэт + внутреннее свечение, луч-цвет, крупные простые формы на тёмном фоне;
  варьируется только субъект. **Субъекты — существа/явления света**, не машины («железный бур» был
  ошибкой — субъект не из мира света). Витраж/преломление — родственный акцентный вариант (кандидат
  для заклинаний/аур). Чистый постер-силуэт — отложен на потом (сплэши/промо). Подробно — `ART.md`.
- **Жесты руки:** драг = разыграть (на стол/цель), **двойной тап = в ману**, клик по заклинанию = прицел.
  Ушли от «клик играет существо» и от drop-зоны «в ману» (драг туда конфликтовал со стрелкой прицела).
- **Счётчик «отложено: N» убран** — голое число неинформативно (не видно ЧТО и КОГДА сработает).
  Если возвращать — делать как «Через N ход(ов): <эффекты>»: выставить содержимое очереди `delay`
  в протоколе (turnsLeft + action/selector/value), рендерить через `Glossary.effect_text`.
- **Временная мана видна** — пипсы по `max(crystals, available)`, бонус (фотосинтез) с зелёным кольцом.
- **Выбор цвета маны — радиальный** (`radial_picker.gd`): кольцо из секторов по числу цветов,
  фиксированное место под любое число цветов; центр-дырка = отмена. Есть тестовая 5-цветная
  карта `prismatic_seraph` (все 5 пипов) для проверки.

## Вырезанные ключевики (сохранены как кандидаты)

Причина у большинства — дубль/слабая связь с цветом/ниша. Можно вернуть позже.

**🔴 Красный**
- **Проникновение** `penetrate` — «урон игнорирует Броню/щиты». Сильный, но зависит от частоты брони.
- **Глубокий след N** `deepwound` — «−N к HP навсегда». Пересекается с Неугасимостью.

**🟡 Жёлтый**
- **Озарение** `reveal` — разовое раскрытие заготовок (дубль Прожектора-ауры).
- **На виду N** `exposed` — «нельзя спрятать + N урона». Мутно при «вся мана скрыта».
- **Блик** `glare` — «получив урон, ослепляет атакующего». Третий вариант ослепления.

**🟢 Зелёный**
- **Семя** `seed` — «при смерти вернись ростком». Дубль Спор по «смерть→токен».
- **Разрастание N** `proliferate` — «токен каждый ход». Медленный токен-мейкер.
- **Цветение N** `bloom` — «если N+ кристаллов: бонус». Дубль Резонанса по «награда за пул».
- **Удушение** `choke` — «вражеский кристалл → твой росток». Ниша (атака по мане).

**🔵 Синий**
- **Хрупкость** `brittle` — «замороженное существо получает двойной урон». Вырезано:
  если существо заморожено, оно и так нейтрализовано (не бьёт), доп. урон по нему бессмыслен.
- **Оцепенение N** `mass_freeze` — масс-заморозка (редкая AoE).
- **Усыпление N** `sleep` — «пропускает ходы, просыпается от урона». Близко к Заморозке.
- **Бессонница** `insomnia` — аура «враг не лечится / без старт-эффектов». Ниша-контра.
- **Всевидение** `insight` — «видите руку/заготовки врага». Инфо, пересекается.

**🟣 Фиолетовый**
- **Фантом** `phantom` — «вообще нельзя выбрать целью». Сильный, пересекается с Незримостью.
- **Порча N** `corrupt` — «−N/−N». Генерик-дебафф, слабо «фиолетовый».

**Из ранних итераций (концепты, не ключевики):**
- **Мимикрия** (выглядеть как другая карта) — ломалась «если нет других карт».
- **Двойник / Клон** — копирует существо, теряя себя.
- **Превращение / Полиморф** (в 1/1) — одиночный эффект без синергий; если нужен — уникальный текст карты.
- Огненная ветка Красного (Пережог/Channel, Угли, Костёр, Горнило, Перегрев, Bloodlust, Haste, Windfury, Reckless, Executioner, Frenzy) — заменена на «красный свет».

## Статус разработки (роадмап клиент+движок)

- ✅ **A — game-feel**: анимации урона/смерти/призыва, неон-стрелка прицеливания, рефлоу борда.
- ✅ **B — базовый UX**: веер руки, экран конца игры, **мулиган**, тултипы с глоссарием.
- 🔶 **C — ключевики ядра**: реализовано 7/9 — ward, dispel, germinate (лёгкие);
  floodlight, delay, decoy («Подготовка»), scry (средние). Осталась тяжёлая пара —
  **ambush** и **refract** (реактивные перехваты боя/таргета).
- 🔶 **D — сетевая игра с друзьями**: **примитивная версия работает** — сервер слушает все
  интерфейсы, сид рандомный на каждую партию, два клиента подключаются по `ws://<host>:8080`
  (LAN — по IP хоста, интернет — порт-форвард/туннель). Осталось: лобби/комнаты по коду,
  переподключение, одна партия за запуск (сервер выходит при дисконнекте), раздача клиента
  (Godot-бинарь/веб-экспорт).
- 🔶 **E — герои + силы героя**: модель «герой = `CardDef` (`type:hero`) + **пассивный** ключевик».
  Реализованы 3 героя-художника с пассивками: **Ирида/Призма** (`spectral_shift` — раз в ход
  кристалл как соседний цвет), **Эреб/Затмение** (`umbra` — −1 урон по герою от существ),
  **Кьяра/Линза** (`clairvoyance` — видно верх своей колоды). Герой назначается **случайно
  на старте** (в сервере). Осталось: экран выбора героя (→ с лобби, фаза D), ростер >3,
  арты-портреты (`ART_HEROES.md`).
- ⬜ **админка/debug-панель** (см. «Инструменты разработки»).

## Технический долг / рефакторинг

- ✅ Вынесены `UiCard` и `FxLayer` из `Main.gd` в `ui_card.gd` / `fx_layer.gd` (через `class_name`).
- ⬜ `Main.gd` ещё ~1885 строк. Кандидаты на вынос: построитель тултипа, визуал карты,
  view-query хелперы — но они завязаны на `self/view/cards`, нужен аккуратный план (не просто перенос).
- ⬜ **Дедуп UI-построителей**: ~28 `Label.new()` с повторяющимися `font_size`/`font_color`
  оверрайдами → хелпер `_label(text, size, color, center)`; то же для часто повторяемых StyleBox.
- ⬜ **Движок (низкий приоритет, меняет поведение — только с тестами):**
  - **awaken-скидка** применяется лишь к `generic`: карта с `generic:0` + цветной стоимостью
    не получает скидку за «свой» кристалл. Дизайнерский вопрос (скидывать с цветной части?).
  - `buffStats`: `c.atk += n` дублирует `recomputeContinuous` (сейчас — safety net; убирать с тестом).

## Решено (геймплей)

- **Мулиган** — реализован end-to-end (движок: фаза мулигана; протокол: action `mulligan` + флаг
  в view; сервер: проброс; клиент: оверлей выбора карт). Первый ход стартует, когда оба подтвердили.
- **Freeze vs Blind разведены** (были механически одинаковы — обе только блокировали атаку):
  **Заморозка** = не атакует, но в защите бьёт в ответ. **Ослепление** = не наносит боевой урон
  вообще (ни в атаке, ни в ответ) → ослеплённого защитника можно убить бесплатно. `Слепящий Иней`
  перетюнен на **blind 1 + freeze 2** (иначе freeze был бы покрыт первым ходом ослепления).

## Идея-кандидат: ось различия — ГЕРОЙ, а не колода

**Суть (предложение пользователя).** Перевернуть, что именно делает партии разными.
Сейчас ось различия — **колода** (личная палитра, колодостроение). Кандидат: у всех игроков
**одна общая колода** (или **выбор из нескольких готовых пресетов**), а весь «прикол» и глубина
переезжают в **героя** — и у героя теперь **несколько способностей** (кит), а не одна пассивка.

**Что это меняет:**
- **Колода:** общая/пресетная → ноль или минимум колодостроения. Балансить радикально проще
  (мало переменных), вход для новичка легче, фокус — на пилотировании, а не на сборке.
- **Герой = билд.** Глубина и реиграбельность идут от **кита героя** (несколько абилок), их
  синергии и тайминга. Это **возрождает активные силы героя**, но в другой рамке: не одинокая
  «HS-кнопка за ману» (её отвергали как клон), а **набор** (например: 1–2 активки + пассивка,
  или малое «древо»/выбор абилок). Различие героев должно быть в КИТЕ, а не в одной черте.
- **Текущая модель героя** (`type:hero` + один пассив-ключевик, рандом на старте) — частный
  случай: расширяется до **списка** ключевиков/активок на герое. Движок уже читает ключевики
  героя в нужных точках; активкам нужен триггер «Активка героя» (плати/таргет, как у карт-активок).

**Открытые вопросы (решить до реализации):**
- Одна общая колода ИЛИ выбор из N пресетов? (N пресетов = немного стратегии сборки сохраняется.)
- Сколько абилок у героя и каких типов (активки/пассивки/выборы)? Стоимость активок (мана/раз-в-ход/заряды)?
- Остаются ли 5 цветов как палитра ОБЩЕЙ колоды, или цвет теперь тоже завязан на героя?
- Совместимо ли это с «мана из своих карт» (общая колода → у всех одинаковый мана-материал)?

**Статус:** кандидат-направление, **не реализуем сейчас**. Влияет на роадмап героев (E) и
дека-модель. Если принимаем — это смена идентичности игры; зафиксировать решением до контента.

## Решено (на текущей сессии)

- **MVP — все 5 цветов** (не подмножество).
- **Нейтральных ключевиков нет**, но есть **малый пул нейтральных (белых) карт** —
  незаменимые базовые эффекты (выметание / устранение / добор / простые тела) на
  примитивах, бесцветная стоимость, доступны любой колоде. Основа колоды — цветная (см. EFFECTS).
- **Покрытие по носителям** не добиваем искусственно: если цвету нужно Заклинание/Аура —
  это делает **конкретная карта** на примитивах, отдельный ключевик ради «слота» не вводим.
- **Герой:** в MVP — **один герой-заглушка**, сила «ничего не делает» за 0 маны. Ростер с
  реальными силами — пост-MVP.
- **Моно-награда (Colorbound)** — пока не делаем (кандидат).
- **Пейофф иллюзий (🟣)** — пока не делаем (кандидат).
- **Числа** — стартуем с дефолтов (HP героя 30 и т.п.), тюним по ходу.
- **Сеттинг** решён (мир света/спектра, Мега-Призма, Испытание Мастерства).

## Ещё открыто

- **Архетип «бей по своим»** (жертвоприношение, «при получении урона…», авто-баффы от
  ран): наносить урон/дебаффы своим существам может быть полезно. Движок **уже готов** —
  селекторы `chosen_friendly_minion` / `chosen_any_minion` работают с любым действием
  (damage/freeze/blind/…). Это чистый контент: задать у карты нужный селектор, доработки
  движка нет. Сейчас beneficial-по-своим есть только у возврата (Зыбкое Смещение = любой
  таргет); остальные заклинания намеренно enemy-only (бить своё = self-harm без архетипа).
- **«Мана — контестед-ресурс» — отложено** (убрано из §3: нет эффектов). Если возвращать —
  нужны ключевики **атаки по мане** (mana-burn / Saboteur), **перекраски** (Convert),
  **сжигания своей** (Пережог) и приписка цвету. Пока не в дизайне.
- **DEF убрали (решено).** Была почти рудиментом (ни один ключевик не использовал) и
  грузила UI третьей цифрой. Перешли на **2 стата ATK/HP**, бой — взаимный ATK (как HS).
  Стена-роль теперь = много HP + Маяк. Баффы «+N/+N» = +N ATK и +N HP.
- **Числовой баланс:** кривые стоимостей, «ванильная» стат-линия, компенсация второго
  игрока, редкость/лимиты сильных (Вспышка, Оцепенение, Фантом).
- **Сеттинг — почти закрыт** (мир **Prisma**, визуал кристаллический/гранёный, существа
  любой формы). Осталось: пайплайн генерации артов (подписка GPT/gpt-image), анатомия
  карты/рамки, и **система рангов** (её высший ранг = титул Испытания Мастерства).

## Инструменты разработки

- **Админка / отладочная панель** — задавать любой сценарий без долгой игры: выставить
  доску (свои/вражеские существа с нужными статами/статусами), ману любого цвета, HP/броню
  героев, руку и колоду, чей ход; форсить розыгрыш/смерть, прокликивать триггеры. Цель —
  быстро воспроизводить кейсы (заморозка, провокация, bypass, иллюзии, ауры) для теста UI и
  правил. Вероятно: спец-команды в протоколе (`debug/setState`), доступные только в dev-сборке.

## Пост-MVP (фичи)

- Web-экспорт клиента (Godot → HTML5) и деплой на VPS + комнаты по коду (играть удалённо).
- Колодостроитель.
- Полные сеты карт по всем 5 цветам; больше героев.
- Система рангов / прогрессия (высший ранг = титул Испытания Мастерства).
- Lua-«аварийный люк» (sol2) для экзотических эффектов, если грамматики не хватит.
- Позиционная боёвка — **решено: НЕ делаем** (как HS); позиционные эффекты отложены.
