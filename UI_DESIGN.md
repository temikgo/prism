# Prism — дизайн интерфейса (ТЗ к переделке клиента)

> Цель: интерфейс в нашем стиле — **живой свет на глубокой тьме** — где **арты главные**,
> а взаимодействие максимально удобное. Жёсткий приоритет качества (частые ошибки, которых
> НЕ должно быть): **перекрывающиеся элементы** и **текст, вылезающий за рамки**. Этот файл —
> источник правды по UI; реализуется по фазам (порядок ниже). Реализацию начинаем по команде.

## 0. Принципы

1. **Свет = смысл.** Состояния и действия показываем светом (свечение, тускнение, иней,
   луч), а не чужеродными иконками/рамками. Тёмный фон, арты светятся изнутри.
2. **Арт — герой кадра.** Карта = почти целиком арт; UI-хром минимален и не заслоняет лицо.
3. **Информация по слоям доступа:** на столе — только то, что нужно в бою (цена, статы,
   статусы, имя). Полные **правила — в зум-попапе** (наведение/удержание), где есть место.
   Так маленькая карта НИКОГДА не переполняется текстом.
4. **Постоянный размер.** Каждый элемент карты привязан к углу/краю с фиксированным размером
   и отступом → лицо карты всегда одного размера независимо от контента (как уже сделано).
5. **Без эмодзи** в UI и игровом тексте (только слова и SVG-иконки) — устоявшееся правило.

## 1. Инварианты против наложений и вылезаний (чек-лист на каждый элемент)

Любой UI-узел обязан удовлетворять, иначе не мёржим:

- **Зоны не пересекаются.** Лицо карты разбито на непересекающиеся зоны (см. §2). Элемент
  живёт строго в своей зоне; угловые гемы и нижняя плашка делят низ так, что не лезут друг
  на друга (плашка инсетится от углов на ширину гема).
- **Текста на лице карты НЕТ.** Имя и правила показываются **только при наведении** (тултип/
  зум-попап), где есть место и автоперенос. На лице — только числа (цена/статы) в гемах-чипах.
  Так лицо карты в принципе не может переполниться текстом (эллипсис на длинных именах выглядел
  плохо — отказались). Никаких `Label`, способных растянуть родителя.
- **Счётчики имеют потолок.** Иконки статусов: максимум **4** видимых, дальше чип `+N`.
  Цветные пипы цены: до 5; капсула цены имеет max-ширину, пипы переносятся внутри неё.
- **Оверлеи не меняют раскладку.** Состояния (иней/сон/подсветка) — это `modulate`/обводка/
  полупрозрачный слой поверх арта, НЕ новые элементы в потоке.
- **Поповеры держатся в экране.** Зум/тултип clamp-ятся по краям вьюпорта (никогда не уходят
  за экран; если у верхнего края — раскрываются вниз, и наоборот).
- **Z-порядок фиксирован** (снизу вверх): арт → скрим-градиенты → обводка-свечение →
  плашка-имя → гемы/цена/статусы → оверлеи состояний → (поверх всего) поповеры/пикеры/стрелка.

## 2. Анатомия карты (лицо)

Карта портретная (бой/рука ≈ 150×210, зум крупнее, пропорции те же — арты 2:3). Слои:

```
┌──────────────────────────┐  ← скруглённый клип; тонкая ЦВЕТНАЯ обводка-свечение по краю
│ [cost]            [stat]  │  ← ВЕРХНЯЯ ЗОНА (высота = гем): слева капсула цены,
│                          │    справа ряд статус-иконок (≤4, потом +N). Лёгкий
│                          │    верхний скрим для читабельности на светлом арте.
│         А Р Т            │  ← АРТ во весь кадр (KEEP_ASPECT_COVERED, под всем)
│      во весь кадр        │
│                          │
│ ░░░░░░░░░░░░░░░░░░░░░░░░ │  ← НИЖНИЙ СКРИМ (градиент в темноту) — только ради читабельности
│[ATK]              [ HP ] │  ← НИЖНЯЯ ЗОНА (высота = гем): ATK слева, HP справа, центр пуст
└──────────────────────────┘     (имя НЕ на лице — показывается при наведении)
```

Зоны по вертикали: **верхняя полоса** (h=GEM) · **арт** (растягивается) · **нижняя полоса
гемов** (h=GEM). Имени на лице нет (см. §1) — оно в тултипе/зуме при наведении.

Элементы:
- **Цена** (верх-слева): число generic + цветные пипы. Капсула «матовое стекло», свечение
  пипа в цвет. Max-ширина; при 5 пипах пипы переносятся в 2 ряда внутри капсулы.
- **Статусы** (верх-справа): SVG-иконки (заморозка/ослепление/щит/нимб/провокация/…), фикс.
  размер, выравнивание вправо, рост влево; потолок 4 + чип `+N`.
- **Имя** на лице НЕ рисуем — только при наведении (тултип/зум). Длинные имена не влезают, а
  эллипсис/мелкий шрифт выглядят плохо; узнаём карту по арту, имя — на расстоянии одного ховера.
- **ATK/HP-гемы** (нижние углы): кристаллы-капсулы со свечением. HP зелёный при полном, тёплый
  при ранении. Рисуются последними (поверх плашки) → всегда читаемы.
- **Правил на лице карты НЕТ.** Они в зум-попапе (§4).

## 3. Состояния — через свет (оверлеи, без сдвига раскладки)

- **Болезнь призыва** («ещё не разгорелось»): арт пригашен и обесцвечен, медленный тусклый
  пульс; в готовый ход — разгорается.
- **Готов к атаке:** тёплая обводка-свечение по краю (анимированная яркость).
- **Заморозка:** холодный кристаллический иней по краю + лёгкий сине-голубой тон поверх арта.
- **Ослепление:** блёклый «засвеченный» тон (детали тонут в белом), без боевого свечения.
- **Цель доступна** (во время атаки/каста): яркий пульс обводки.
- **Выбрана/атакует:** подъём + усиленное свечение.
Всё это — `modulate`/обводка/полупрозрачный слой; элементы те же, наложений не добавляют.

## 4. Зум-попап и тултип (единственное место полных правил)

- **Наведение/удержание** на карту (рука, стол, верх колоды) → крупный попап: лицо карты в
  большом размере + **сгенерированные правила** (из keywords+effects, `Glossary`) + поясняшки
  статусов. Коробка правил автопереносит и растёт по контенту — переполнения нет by design.
- Попап **clamp-ится в экран** (у края — раскрывается в противоположную сторону). Поверх всего.
- Тач/долгий тап = тот же попап (для будущего тач-таргета).

## 5. Стол и атмосфера

- **Фон:** глубокая тьма с лёгким **спектральным свечением Мега-Призмы** (наш сеттинг) —
  мягкий радужный градиент-«ореол» сверху-по-центру, очень деликатно (не спорит с артами).
- **Дрейфующие световые мошки** — редкие, медленные (дешёвые частицы), задают «живой свет».
- **Полки существ:** под каждым рядом тонкая светящаяся линия-«ледж» + слабое отражение —
  существа стоят в освещённом пространстве, а не висят на плоскости.
- **Раскладка остаётся:** половины `[медальон героя | ряд существ | стопки колода/сброс/мана]`,
  враг сверху, ты снизу (схема «фланговые медальоны» — уже принята). Между половинами —
  тёмная «нейтральная полоса» со светом Призмы; туда же ложится стрелка прицела.
- **Перенос рядов по ширине** (до лимита 8) без раздувания — фикс. размеры карт, перенос.

## 6. Герой (дуэлянт-художник)

- Квадратный портрет 1:1 в скруглённой раме с цветной обводкой-свечением (сигнатурная палитра
  героя). Крупнее, чем сейчас, но в пределах фланга.
- **HP — крупный кристалл-гем** на медальоне; **броня** — меньший гем рядом (показывать только
  при >0). **Пассив** — SVG-иконка-чип с тултипом (название + текст пассива).
- Имя героя — одно слово под портретом. Вражеский медальон — дроп-цель «в лицо» с явным
  свечением при наведении валидной атаки.

## 7. Рука, перетаскивание, прицел

- **Веер снизу:** карты слегка веером; наведение — подъём + лёгкий зум + свечение + зум-попап
  (§4). Перекрытие соседей при наведении убирается (поднятая карта поверх).
- **Жесты (уже приняты):** драг = разыграть/прицелиться (световая лента-стрелка), двойной тап =
  в ману (радиальный пикер цвета для многоцветных), клик по заклинанию = режим прицела.
- **Стрелка прицела** — чистая световая лента в цвет действия, с мягким наконечником; лежит в
  нейтральной полосе/поверх всего, не цепляет карты.

## 8. Типографика и палитра

- Единая **шкала кеглей** (заголовок/имя/число/подпись) вместо разрозненных `font_size`.
- Палитра: глубокий тёмный база + 5 цветовых акцентов лучей (из `Palette`); неон-свечения
  умеренные. Хелперы `_label(text,size,color,center)` и общие StyleBox — чтобы не плодить.

## 9. Микроанимации (свет)

- Выход существа — короткая вспышка/собирание света. Атака — рывок + след-луч. Смерть —
  рассеивание в искры. Урон по герою — вспышка по краю медальона. Добор — карта «зажигается».
- Всё короткое (game-feel), не блокирует ввод.

## 10. Порядок реализации (фазы; каждая — самодостаточный заход с проверкой)

1. **Каркас лица карты + состояния-через-свет.** Зоны §2, хелперы (`_label_clamped`,
   общие StyleBox), полноэкранный арт + обводка-свечение + нижний скрим + плашка-имя; правила
   убрать с лица в зум. Состояния §3. — корень и читабельности, и анти-переполнения.
2. **Зум-попап + рука.** Hover-зум с полными правилами (clamp в экран); веер/подъём.
3. **Атмосфера стола.** Фон, свет Мега-Призмы, полки существ, мошки.
4. **Герой.** Портрет/кристалл-HP/броня/иконка пассива/дроп-в-лицо.
5. **Стрелка + микроанимации + типошкала/палитра** — финальная полировка.

После каждой фазы — твоя визуальная проверка, потом следующая.
